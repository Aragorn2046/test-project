Build a simple hello world app.

## Before You Start
1. Read `CLAUDE.md` in this directory for project rules and conventions.
2. Run `export PATH="$HOME/bin:$PATH"` to ensure `gh` CLI is available.
3. Initialize git and create the GitHub repo as your FIRST step:
   ```bash
   bash ~/scripts/spinoff-git-init.sh test-project ~/projects/test-project
   ```

## Task
Create a simple hello world application. Choose an appropriate language/framework (a basic Python or Node.js script is fine). The app should:
- Print "Hello, World!" when run
- Include a brief README.md
- Be clean and minimal

## Workflow
- After each major step, update `status.md` with what was done, current state, and next steps.
- When finished, commit all work and push to GitHub.
- Append a session summary to `/home/arago/vault/Claude Knowledge Base/Session Log.md`.
